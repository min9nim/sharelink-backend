{
  "name": "sharelink-jsonserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "json-server": "^0.14.0",
    "request": "^2.88.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js -p $PORT",
    "dev": "./db-backup.sh && node server.js -p 3030",
    "devmon": "./db-backup.sh && nodemon server.js -p 3030"
  },
  "author": "",
  "license": "ISC"
}
